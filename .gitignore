node_modules/
.next/
out/
build/
.env*.local
.env
.DS_Store
*.pem
coverage/
next-env.d.ts

supabase/.temp/

.env.local.backup-prod
.env*.backup*

# WEB-003 Registry — les .md sont la source versionnée ; les .pdf (artefacts de
# publication) et les .zip (artefacts de transfert) restent hors historique.
docs/web/registry-import/**/*.pdf
docs/web/registry-import/**/*.zip

# OCR-GROUND-001 — sorties de test du générateur (régénération vs golden), jamais committées.
build/

# Config d'outillage locale (preview dev) — comme .claude/settings.local.json (ignore global).
.claude/launch.json
